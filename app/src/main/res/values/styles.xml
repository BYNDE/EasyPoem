<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppSearchView" parent="Widget.AppCompat.SearchView" >

        <!-- Query Text Color -->
        <item name="android:textColorPrimary">@color/icon_active</item>
        <!-- Hint Color -->
        <item name="android:textColorHint">@color/icon_active</item>
        <!-- Icon Color -->
        <item name="android:tint">@color/icon_active</item>
    </style>


    <style name="Theme.MyApplication" parent="Theme.AppCompat.Light">
        <item name="actionBarStyle">@style/Theme.MyApplication.ActionBar</item>
    </style>


    <style name="Theme.MyApplication.ActionBar">
        <!-- <item name="android:elevation">0dp</item> -->
        <!-- if you are using support library thn elevation should be without android namespace like below else the above one uncomment-->
        <item name="elevation">0dp</item>
    </style>

    <style name="StartTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/drawable</item>
    </style>

</resources>